{
  "name": "fch-micro-front",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:device": "lerna run fch:start --scope=sub-device --stream",
    "start:master": "lerna run fch:start --scope=master --stream",
    "start:fence": "lerna run fch:start --scope=sub-electric-fence --stream",
    "start:upms": "lerna run fch:start --scope=sub-upms --stream"
  },
  "workspaces": [
    "packages/*"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^7.3.1"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "npm-run-all": "^4.1.5"
  }
}
